const GATEWAY_HOST = "http://localhost";

export const HOST = {
  user_service: `${GATEWAY_HOST}/api/users`,  
  auth_service: `${GATEWAY_HOST}/api`,
  device_service: `${GATEWAY_HOST}/api/devices`,
  my_device_service: `${GATEWAY_HOST}/api/my-devices`,
};